[project]
name = "screengrab-demo"
version = "0.1.0"
description = "Streamlit demo for classifying CRM automation failures from screenshot sequences."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0",
    "pillow>=10.0",
    "streamlit>=1.36",
]

[tool.uv]
package = false
